import SeoPDP from "$store/components/seo/SeoPDP.tsx";
import type { Props } from "$store/components/seo/SeoPDP.tsx";

function SeoPDPSection(props: Props) {
  return <SeoPDP {...props} />;
}

export default SeoPDPSection;
